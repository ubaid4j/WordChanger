use_gpu = no
use_gpu=no
use_gpu="no"
use_gpu="no"
use_gpu=no
use_gpu="no"
use_gpu = "no" #use_gpu=no
use_gpu = "no"
use_gpu="no" #use_gpu=no
use_gpu="no"
use_gpu="no" #use_gpu=no
use_gpu = "no"
use_gpu = no #use_gpu=no
use_gpu="no"
use_gpu="no"
